import 'package:flutter/material.dart';

class FlutterMovieDB extends StatelessWidget {
  const FlutterMovieDB({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}